# このファイルは「変数名のテンプレート」である。値は絶対に書き込まないこと。
# 実際の値は .env（Git 追跡外）か、シェルの環境変数に設定する。
#
#   PowerShell:  $env:PIXELLAB_API_KEY = "..."
#   bash:        export PIXELLAB_API_KEY="..."

# PixelLab API キー
PIXELLAB_API_KEY=
