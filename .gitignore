# =====================================================================
# .gitignore
#
# 除外パターンの方針:
#   refs/ と _work/ は「**/ 付きのパターン」で全階層を一括除外する。
#   projects/*/refs/ のような限定パターンにすると templates/project/refs/ や
#   将来 projects/ 以外に置かれたディレクトリを取りこぼすため。
#   詳細は docs/CONVENTIONS.md を参照。
# =====================================================================

# ---------------------------------------------------------------------
# 機密情報
#   API キーは環境変数から読む。リポジトリには一切置かない。
#   .env.example は変数名のみを記載したテンプレートなので追跡する。
# ---------------------------------------------------------------------
.env
.env.*
!.env.example

# ---------------------------------------------------------------------
# 参考画像（第三者の著作物）
#   既存商用ゲームのスクリーンショット等が置かれる。
#   例外（! による再包含）は一切設けない。
# ---------------------------------------------------------------------
**/refs/

# ---------------------------------------------------------------------
# 中間生成物（API の生出力）
#   Git にも LFS にも入れない。再現性はログのシードとプロンプトで担保する。
#   例外（! による再包含）は一切設けない。
# ---------------------------------------------------------------------
**/_work/

# ---------------------------------------------------------------------
# Python
# ---------------------------------------------------------------------
__pycache__/
*.pyc
*.pyo
*.pyd
.venv/
venv/
.pytest_cache/

# ---------------------------------------------------------------------
# OS 由来
# ---------------------------------------------------------------------
.DS_Store
._*
Thumbs.db
ehthumbs.db
desktop.ini
_preview_sheets/
